import styled from 'styled-components';

const TextAreaBox = styled.div`
  .am-textarea-label {
    font-size: 15px;
    color: #323233;
  }
  .am-textarea-control {
    textarea {
      font-size: 15px;
    }
  }
`;

export default TextAreaBox;
